gpu:
  index: 0
  clocks_mhz: []
  sample_interval_ms: 100

benchmark:
  job_name: baseline_natural_max
  command: "python benchmarks/pytorch_workload_bench.py --kind mixed --size 4096 --repeats 50 --steps 10 --warmup-steps 1"
  warmup_seconds: 2
  repeats: 5

output:
  root_dir: data/raw
